import { HeaderContainer } from "./Header.styles";

const Header = () => {
    return (
        <HeaderContainer>
            <h1>Infinite Auction for Ukraine</h1>
            <h2>An NFT collection sold at auction that never ends. All funds go to Ukraine.</h2>
        </HeaderContainer>
    );
};

export default Header;
