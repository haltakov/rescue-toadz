import { HeaderContainer } from "./Header.styles";

const Header = () => {
    return (
        <HeaderContainer>
            <h1>
                <a href="/">Infinite Auction for Ukraine</a>
            </h1>
        </HeaderContainer>
    );
};

export default Header;
